name: Release ABB
run-name: ${{ github.actor }} is Building ABB 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: yarn install
      - name: Grant primission
        run: chmod +x ./android/gradlew
      - name: Build app
        run: yarn releaseABB
      - name: Get current timestamp
        run: echo "::set-env name=TIMESTAMP::$(date +%s)"
      - name: Rename ABB
        run: mv android/app/build/outputs/bundle/release/app-release.abb android/app/build/outputs/bundle/release/dmw-${{ env.TIMESTAMP }}.abb
      - name: Deploy to GitHub
        uses: actions/upload-artifact@v1
        with:
          name: dmw-${{ env.TIMESTAMP }}.apk
          path: android/app/build/outputs/bundle/release/dmw-${{ env.TIMESTAMP }}.abb
